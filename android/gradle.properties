org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Kotlin's incremental compiler crashes (IllegalArgumentException: "this and
# base files have different roots") when the project (D:) and the pub cache
# (C:) sit on different drives, which is how android_file_picker's sources
# get resolved. Non-incremental compiles are slower but don't hit this path.
kotlin.incremental=false
