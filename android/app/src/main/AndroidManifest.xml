<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prodktstudio.tayebqalbak">
    <!-- Android 13+ requires this at runtime before any notification can
         show — NotificationScheduler requests it the first time a reminder
         is switched on (lib/data/services/notification_scheduler.dart). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- SCHEDULE_EXACT_ALARM is deliberately NOT declared. Google Play
         restricts it to apps whose core function is an alarm clock, timer or
         calendar; a daily reminder does not qualify, and declaring it invites
         a policy rejection at review. NotificationScheduler already handles
         its absence: canScheduleExactNotifications() returns false and every
         reminder is scheduled inexactAllowWhileIdle, which the OS may batch
         into a maintenance window. For an 08:30 spiritual reminder that
         window is not something a reader would notice, and it is the same
         path the app already took on any device where the grant was
         declined. -->
    <!-- Lets ScheduledNotificationBootReceiver re-arm reminders after a
         device reboot (the ones AlarmManager drops on power-off). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="طيّب قلبك"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Flutter Embedding V2 Declaration -->
            <meta-data
              android:name="flutterEmbedding"
              android:value="2" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- flutter_local_notifications (18.x) no longer registers these in its
             own manifest — without them here, AlarmManager fires but there's no
             receiver component to deliver the broadcast to, so scheduled
             reminders silently never show. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
