<?xml version="1.0" encoding="utf-8"?>
<!-- Same branded launch window as drawable/launch_background.xml; see the
     reasoning there. Kept as its own file so the night case stays explicit
     rather than silently inheriting. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FF15362B" />
</shape>
